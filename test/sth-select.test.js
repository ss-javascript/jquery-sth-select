/*
 * Dependencies
 */
const assert = require("chai").assert;

describe("SthSelect Test Suite", () => {

	it("First test", () => {
		let letter = "a";
		assert.equal(letter, "a", "`letter` should be equal to `a`");
	});

});